name: CI - Cursor Review

# Thin caller for the shared reusable cursor-review workflow in
# Comfy-Org/github-workflows. The review logic (panel matrix, judge
# consolidation, prompts, extract/post/notify scripts) lives there as the
# single source of truth, so this repo only carries the repo-specific diff
# excludes.

on:
  pull_request:
    types: [labeled, unlabeled]

concurrency:
  group: cursor-review-pr-${{ github.event.pull_request.number }}-${{ github.event.label.name }}
  cancel-in-progress: true

jobs:
  cursor-review:
    permissions:
      contents: read
      pull-requests: write
    # SHA-pinned per zizmor `unpinned-uses: hash-pin`. Bump this SHA to pick up
    # upstream changes; keep `workflows_ref` matching so prompts/scripts load
    # from the same commit as the workflow definition.
    uses: Comfy-Org/github-workflows/.github/workflows/cursor-review.yml@00b0eef5fe3e85422bdb525024bf4b2a3ec92b7d # github-workflows main (00b0eef)
    with:
      workflows_ref: 00b0eef5fe3e85422bdb525024bf4b2a3ec92b7d
      diff_excludes: >-
        :!**/pnpm-lock.yaml
        :!**/package-lock.json
        :!**/yarn.lock
        :!**/node_modules/**
        :!**/.claude/**
        :!**/dist/**
        :!**/vendor/**
        :!**/*.generated.*
        :!**/*.min.js
        :!**/*.min.css
        :!**/*-snapshots/**
    secrets:
      CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
      SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
